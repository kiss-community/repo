diff --git a/meson.build b/meson.build
index 0cf2b3e..51420d2 100644
--- a/meson.build
+++ b/meson.build
@@ -474,8 +474,6 @@ if gperf.found() and get_option('wrap_mode') != 'forcefallback'
     error('unable to determine gperf len type')
   endif
 else
-  # Fallback to subproject
-  gperf = find_program('gperf')
   # assume if we are compiling from the wrap, the size is just size_t
   gperf_len_type = 'size_t'
 endif
diff --git a/src/meson.build b/src/meson.build
index 0f91d4b..a9940f1 100644
--- a/src/meson.build
+++ b/src/meson.build
@@ -58,12 +58,6 @@ fcobjshash_gperf = custom_target(
   build_by_default: true,
 )
 
-fcobjshash_h = custom_target('fcobjshash.h',
-  input: fcobjshash_gperf,
-  output: 'fcobjshash.h',
-  command: [gperf, '--pic', '-m', '100', '@INPUT@', '--output-file', '@OUTPUT@']
-)
-
 lib_fontconfig_sources = [fc_sources, alias_headers, ft_alias_headers, fccase_h, fcconst_h, fclang_h, fcobjshash_h, fcstdint_h]
 lib_fontconfig_kwargs = {
   'include_directories': incbase,
